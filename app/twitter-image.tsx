/*
 * X/Twitter usa la stessa immagine dell'Open Graph. La card è già
 * dichiarata come `summary_large_image` in app/layout.tsx.
 */
export {
  default,
  alt,
  size,
  contentType,
} from "./opengraph-image";
